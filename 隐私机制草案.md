# 有来有往：隐私机制草案

## 一句话原则

在用户主动发起律师咨询并确认材料范围之前，平台不读取、不上传、不向任何第三方传递用户的关系支出记录和截图。

## 数据分层

### 1. 私密记录层

- 关系称呼、关系类型、金额、类目、转账附言、个人备注、截图和小票默认仅保存在用户设备。
- 截图和小票由用户主动选择或拍摄，不读取整个相册；处理和压缩在设备端完成。
- 不读取或同步完整微信支付流水，也不要求用户导入与当前关系无关的消费记录。
- 金额达到提醒线只触发本地提示，不触发上传或联系律师。

### 2. 提醒服务层

- 应用内提醒直接在设备端根据本地记录计算。
- 微信订阅消息或服务号提醒必须由用户单独开启。
- 服务端只保存发送消息必需的标识、订阅状态和到期时间，不保存关系称呼、金额、截图和备注。
- 通知正文使用中性表达，例如“你有一项到期提醒”，敏感详情只在用户进入应用后展示。

### 3. 律师咨询层

- 默认关闭，不因金额、关系类型或系统判断自动开启。
- 用户逐项选择要分享的记录，并分别决定是否包含截图、附言、备注及联系方式。
- 发送前展示完整材料预览、接收律师、使用目的、保存期限和删除方式。
- 用户对本次咨询单独确认后，才向指定律师发送选中副本。
- “开不了口模式”必须先让用户选择具体记录，再展示提交给律所公众号后台的完整留言包；未选中的其他关系和记录不进入提交范围。
- 正式版需要加密传输、访问记录、权限隔离、撤回授权和删除申请能力。

## 用户控制

- 查看本地保存的数据类型、数量与大致空间占用。
- 导出包含完整记录和截图的本地备份。
- 清除全部本地关系、记录与截图。
- 分别关闭关系提醒、微信消息订阅和律师材料共享。

## 微信小程序落地检查

- 上线前在微信公众平台配置与实际处理活动一致的《小程序用户隐私保护指引》。
- 涉及隐私接口时，先展示用途和范围，并在用户同意后调用相应接口。
- 订阅消息由用户主动操作触发，不把订阅同意视为读取账单或共享律师材料的同意。
- 对接任何统计、OCR、存储、电子签或客服 SDK 前，重新核对其收集字段、服务器位置、保存期限和第三方共享情况。
- 隐私指引、产品界面和实际代码必须保持一致；未实现的能力必须明确标注为概念演示或规划。

## 当前原型边界

- 没有业务服务器和账号系统。
- 没有读取微信钱包或聊天记录。
- 用户只能主动添加文字、截图、小票或礼物信息；不存在微信付款流水同步入口。
- 正式小程序即使支持从微信会话多选图片或文件，也只处理用户当次主动选择的项目，不等于读取普通文字聊天历史。
- 没有真实微信订阅消息、服务号推送或律师发送能力。
- “直面模式”和“开不了口模式”目前只生成本地预览；模拟微信转发或模拟提交都不会传出数据。
- 法律小课会打开最高人民法院外部网页，但不会把用户记录附加到链接中。
- 本地浏览器存储未被设计成加密、可靠、可验证的电子证据保险箱。
