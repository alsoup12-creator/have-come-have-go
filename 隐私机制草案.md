# 有来有往：隐私机制草案

本产品是独立的个人记录工具，不代表、代理或专门导向任何律所或律师。

## 一句话原则

记录默认只保存在用户设备。只有用户主动选择具体材料、查看完整预览并确认生成分享副本后，所选内容才可以离开本地；未选择的记录不受影响。

## 数据分层

### 1. 私密记录层

- 关系称呼、关系类型、金额、类目、转账附言、个人备注、截图和小票默认仅保存在用户设备。
- 截图和小票由用户主动选择或拍摄，不读取整个相册；处理和压缩在设备端完成。
- 不读取或同步完整微信支付流水，也不要求用户导入与当前关系无关的消费记录。
- 金额达到提醒线只触发本地提示，不触发上传、分享或联系任何人。

### 2. 提醒服务层

- 应用内提醒直接在设备端根据本地记录计算。
- 微信订阅消息必须由用户单独开启。
- 服务端只保存发送消息必需的标识、订阅状态和到期时间，不保存关系称呼、金额、截图和备注。
- 通知正文使用中性表达，例如“你有一项到期提醒”，敏感详情只在用户进入应用后展示。

### 3. 主动分享层

- 默认关闭，不因金额、关系类型或系统判断自动开启。
- 用户逐项选择要分享的记录，并分别决定是否包含截图、附言、备注及联系方式。
- 用户可以生成两类材料：发给关系相对方的事实确认页，或发给自行选择律师的咨询事实材料。
- 产品不隶属于、不绑定、不推荐任何固定律所或律师，也不把材料自动提交给机构后台。
- 发送前展示完整材料预览、用途、保存期限、撤回和删除方式。
- 跨设备查看需要临时分享服务。用户确认后，仅将所选副本加密上传并生成难以猜测的随机链接；未选中的关系和记录继续留在本地。
- 微信联系人由用户在系统转发界面自行选择。产品不应读取通讯录，也不把“生成分享副本”等同于已经发送成功。
- 正式版需要加密传输、短期保存、访问控制、到期失效、主动撤回和删除能力。

## 用户控制

- 查看本地保存的数据类型、数量与大致空间占用。
- 导出包含完整记录和截图的本地备份。
- 清除全部本地关系、记录与截图。
- 分别关闭关系提醒、微信消息订阅和临时材料分享。

## 微信小程序落地检查

- 上线前在微信公众平台配置与实际处理活动一致的《小程序用户隐私保护指引》。
- 涉及隐私接口时，先展示用途和范围，并在用户同意后调用相应接口。
- 订阅消息由用户主动操作触发，不把订阅同意视为读取账单或对外分享材料的同意。
- 小程序卡片转发必须由用户主动触发。生成跨设备可访问的分享页前，应单独说明哪些字段将被上传、保存多久以及如何撤回。
- 对接任何统计、OCR、存储、电子签或客服 SDK 前，重新核对其收集字段、服务器位置、保存期限和第三方共享情况。
- 隐私指引、产品界面和实际代码必须保持一致；未实现的能力必须明确标注为概念演示或规划。

## 当前原型边界

- 没有业务服务器和账号系统。
- 没有读取微信钱包或聊天记录。
- 用户只能主动添加文字、截图、小票或礼物信息；不存在微信付款流水同步入口。
- 正式小程序即使支持从微信会话多选图片或文件，也只处理用户当次主动选择的项目，不等于读取普通文字聊天历史。
- 没有真实微信订阅消息或跨设备分享能力。
- “和对方说清”和“请律师看看”目前都只生成本地预览；模拟微信转发不会上传或传出数据。
- 法律小课会打开最高人民法院外部网页，但不会把用户记录附加到链接中。
- 本地浏览器存储未被设计成加密、可靠、可验证的电子证据保险箱。
